<% if answer.post.user == current_user && answer.post.best_answer == 0 %>
   <%= link_to "ベストアンサーに選ぶ",posts_best_answer_path(id: answer.post.id, answer_id: answer.id), id: "best_answer_#{answer.id}",class: "bestanswer", remote: true %>
<% end %>
<% if answer.post.best_answer == answer.id %>
    <i class="fa fa-check-square"></i> 
<% end %>

