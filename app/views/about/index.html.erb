
<% if signed_in?  %>
  <div class="row">
    <aside class="span4">
      
      <section>
        <%= render 'shared/user_info' %>
      </section>

      <section>
        <%= render 'posts/form' %>
      </section>
    </aside>
    <div class="span8">
      <% if @feed_posts.any? %>
        <h3>Posts</h3>
        <ol class="microposts">
          <%= render @feed_posts %>
        </ol>
        <%= will_paginate @feed_posts %>
      <% end %>
    </div>
  </div>

<% else %>

<div class="jumbotron">
    <div class="container">
      <h1>CodeMateへようこそ。</h1>
      <p>日本中のプログラマーのためのサイトです。コードを書いていて行き詰まってしまった時、CodeMateがあればもう１人で悩むことはありません。CodeMateにはプログラマーによる悩みとその答えが集約されています。どうぞ自分の問題の解決に役立つ質問を探してみてください。きっと見つかることでしょう。見つからない、そんな時には是非とも新たに質問を投稿して、CodeMateに新たな１ページを付け加えてやってください。</p>
      <p><a class="btn btn-lg midashi-btn" role="button">もっと詳しく &raquo;</a></p>
    </div>
  </div>

  <div class="center hero-unit">
    <h1>Welcome to Codemate!</h1>

    <%= link_to "新規ユーザー登録", new_user_path, class: "btn btn-large btn-primary" %>
  </div>

<% end %>