<%= link_to profile_image_for(current_user), current_user %>
<h4>
  <%= current_user.name %>
</h4>
<span>
  <%= link_to "マイページ", current_user %>
</span>
<span>
  　質問数：<%= pluralize(current_user.posts.count, "") %>
   |
  回答数：<%= pluralize(current_user.answers.count, "") %>
</span>
